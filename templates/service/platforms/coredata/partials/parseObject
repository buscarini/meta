{{#object}}
{{^type_date}}
{{#type_url}}
value = dic[@"{{name}}"];
if ([value isKindOfClass:[NSString class]]) {
	object.{{destination}} = [NSURL URLWithString:value];
}
else {
	{{> logError}}
}
{{/type_url}}
{{^type_url}}
value = dic[@"{{name}}"];
if ([value isKindOfClass:[{{type}} class]]) {
	object.{{destination}} = value;
}
else {
	{{> logError}}
}
{{/type_url}}
{{/type_date}}
{{/object}}