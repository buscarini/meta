{{#type_date}}
value = dic[@"{{name}}"];
if ([value isKindOfClass:[NSString class]]) {
	object.{{destination}} = [{{name}}DateFormatter dateFromString:value];
}
else {
	{{> logError}}
}

{{/type_date}}